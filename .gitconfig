[color]
	ui=auto

[color "grep"]
	match = bold green

[imap]
	folder="Drafts"
	tunnel="ssh -q joshtriplett.org dovecot --exec-mail imap"

[push]
	default=matching

[url "ssh://joshtriplett.org/"]
	pushInsteadOf = "git://joshtriplett.org/"
